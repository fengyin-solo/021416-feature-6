# label-02141 对话记录

## 对话 1：用户需求

### 用户

我现在想要开发一个编辑器核心主要用来实现md即时渲染模式（非双列），即用户能够无损的编辑md文件然后看到渲染效果，当光标所在区域存在语法标记时，展示语法标记，否则展示渲染效果，并且在语法标记和渲染效果切换过程中，用户的体验不能割裂，现在请你在当前目录下帮我开发，最好使用vue

### AI 回复

判定为 Case B（需求明确），核心是一个纯前端 Markdown 即时渲染编辑器，无需后端。

#### 技术方案

- Vue 3 + Vite + SCSS + Pinia
- CodeMirror 6 作为编辑器引擎
- 自研 Markdown 解析器识别语法区域
- CM6 Decoration 系统实现行内渲染/隐藏

#### 核心实现原理

1. **Decoration 系统**：利用 CM6 的 `Decoration.mark` 和 `Decoration.replace` + `WidgetType` 在编辑器内直接装饰文本
2. **光标感知**：通过 `ViewPlugin` 监听光标位置变化，判断光标是否在某个 Markdown 语法节点所在行
3. **平滑切换**：当光标进入/离开语法区域时，通过 CSS transition 实现渲染态和编辑态的平滑过渡
4. **无损编辑**：底层始终保持原始 Markdown 文本，渲染仅是视觉层的 Decoration

#### 项目结构

```
frontend-editor/
├── package.json
├── vite.config.js
├── index.html
├── Dockerfile
├── nginx.conf
├── public/
│   └── vite.svg
└── src/
    ├── main.js
    ├── App.vue
    ├── components/
    │   ├── Toolbar.vue
    │   ├── EditorPane.vue
    │   └── StatusBar.vue
    ├── editor/
    │   ├── index.js
    │   ├── setup.js
    │   ├── theme.js
    │   ├── markdown-parser.js
    │   └── decoration-plugin.js
    ├── stores/
    │   └── editor.js
    └── styles/
        ├── _variables.scss
        ├── global.scss
        └── editor-theme.scss
```

#### 支持的 Markdown 语法

- Heading (h1-h6)
- Bold / Italic / Strikethrough
- Inline Code / Code Block
- Link / Image
- Blockquote
- Unordered / Ordered List
- Task List
- Horizontal Rule

---

## 对话 2：UI 重构

### 用户

用中文进行沟通，将我们的所有对话（包括之后的每一次对话）写入一个label-02141.md中（包括生成的代码），使用 frontend-design的skill 完全重构重新实现frontend-editor 项目的所有页面 UI 和交互（重构 UI 和交互的思路也要写入对话），风格参考世界顶级同类型网站，使用简约风，整体要现代化的样式，各种图标也更换为更合理的图标

### AI 回复

#### 重构设计思路

参考 Typora、Notion、Bear、iA Writer 等世界顶级 Markdown 编辑器的设计语言，确定以下重构方向：

**美学方向：Refined Minimalism（精致极简）**

1. **色彩体系**：抛弃冷灰，采用暖灰色调（stone 系列）+ 单一蓝色强调色 `#2563eb`，营造温暖而专注的写作氛围
2. **字体组合**：
   - UI/标题：**Sora**（几何感现代无衬线）
   - 正文：**Source Serif 4**（优雅衬线体，阅读舒适）+ **Noto Serif SC**（中文衬线）
   - 代码：**JetBrains Mono**
3. **图标系统**：去掉所有 emoji 图标，全部替换为纯 SVG 线条图标（Feather/Lucide 风格），16px 统一尺寸，2px 描边
4. **工具栏**：
   - 高度从 48px 缩减到 44px
   - 按钮按功能分组（文本格式 | 代码/链接/图片 | 块级元素），组间用细线分隔
   - hover 效果：柔和蓝色背景 + 图标变蓝
   - 按下效果：微缩放 `scale(0.93)`
5. **状态栏**：从左右分布改为居中单行，用 `·` 分隔各项信息，高度缩减到 24px
6. **编辑区**：
   - 正文区域最大宽度 720px 居中，营造沉浸式写作
   - 标题使用 Sora 字体 + 负字间距，与正文形成层次对比
   - 代码块使用深色背景 `#18181b`
   - 引用块仅保留 2px 左边框，去掉背景色
   - 分割线简化为 1px 细线
7. **滚动条**：宽度缩减到 5px，几乎隐形
8. **品牌**：编辑器命名为 **Mira**，左上角用 SVG 钢笔图标 + 文字

#### 重构的文件清单

- `src/styles/_variables.scss` — 全新变量系统
- `src/styles/global.scss` — 全局样式 + Google Fonts 引入
- `src/styles/editor-theme.scss` — 编辑器渲染样式
- `src/components/Toolbar.vue` — 纯 SVG 图标工具栏
- `src/components/EditorPane.vue` — 编辑器容器
- `src/components/StatusBar.vue` — 极简状态栏
- `src/App.vue` — 主布局
- `src/editor/theme.js` — CM6 基础主题
- `index.html` — 页面标题
- `vite.config.js` — 构建配置

编辑器核心逻辑文件（`markdown-parser.js`、`decoration-plugin.js`、`setup.js`、`stores/editor.js`）保持不变，仅重构 UI 层。

#### 生成的代码

##### `src/styles/_variables.scss`

```scss
// === Palette — warm neutral + single accent ===
$accent: #2563eb;
$accent-soft: rgba(37, 99, 235, 0.08);
$accent-mid: rgba(37, 99, 235, 0.15);

$bg: #fafaf9;
$bg-editor: #ffffff;
$bg-elevated: #ffffff;
$bg-code: #f4f4f5;
$bg-code-block: #18181b;

$text: #1c1917;
$text-2: #57534e;
$text-3: #a8a29e;
$text-code: #e879f9;

$border: #e7e5e4;
$border-light: #f5f5f4;

$success: #16a34a;
$warning: #d97706;
$error: #dc2626;

// === Typography ===
$font-ui: 'Sora', system-ui, -apple-system, sans-serif;
$font-body: 'Source Serif 4', 'Noto Serif SC', Georgia, serif;
$font-mono: 'JetBrains Mono', 'Fira Code', ui-monospace, monospace;

$fs-xs: 11px;
$fs-sm: 13px;
$fs-base: 16px;
$fs-lg: 18px;

$lh: 1.8;

// === Spacing ===
$sp-1: 4px;
$sp-2: 8px;
$sp-3: 12px;
$sp-4: 16px;
$sp-5: 24px;
$sp-6: 32px;
$sp-8: 48px;
$sp-10: 64px;

// === Radius ===
$r-sm: 4px;
$r-md: 6px;
$r-lg: 10px;
$r-full: 9999px;

// === Shadows ===
$shadow-sm: 0 1px 2px rgba(0,0,0,0.04);
$shadow-md: 0 2px 8px rgba(0,0,0,0.06);
$shadow-lg: 0 8px 24px rgba(0,0,0,0.08);

// === Motion ===
$ease: cubic-bezier(0.4, 0, 0.2, 1);
$t-fast: 0.12s;
$t-normal: 0.2s;
$t-slow: 0.3s;

// === Z-index ===
$z-toolbar: 100;
$z-toast: 500;
```

##### `src/styles/global.scss`

```scss
@use 'variables' as *;

@import url('https://fonts.googleapis.com/css2?family=Sora:wght@400;500;600;700&family=Source+Serif+4:ital,wght@0,400;0,600;0,700;1,400&family=JetBrains+Mono:wght@400;500&family=Noto+Serif+SC:wght@400;700&display=swap');

*,
*::before,
*::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  font-size: $fs-base;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  text-rendering: optimizeLegibility;
}

body {
  font-family: $font-ui;；
  color: $text;
  background: $bg;
  line-height: $lh;
  overflow: hidden;
  height: 100vh;
}

#app {
  height: 100vh;
  display: flex;
  flex-direction: column;
}

::-webkit-scrollbar { width: 5px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb {
  background: $text-3;
  border-radius: $r-full;
  &:hover { background: $text-2; }
}

::selection { background: $accent-mid; }

button { font-family: inherit; }
```

##### `src/styles/editor-theme.scss`

```scss
@use 'variables' as *;

.cm-editor {
  height: 100%;
  font-family: $font-body;
  font-size: $fs-base;
  line-height: $lh;

  &.cm-focused { outline: none; }

  .cm-scroller {
    overflow-y: auto;
    padding: $sp-8 $sp-5;
    font-family: inherit;
  }

  .cm-content {
    max-width: 720px;
    margin: 0 auto;
    padding: 0;
    caret-color: $accent;
  }

  .cm-line { padding: 1px 0; }
  .cm-cursor { border-left: 1.8px solid $accent; }
  .cm-selectionBackground { background: $accent-mid !important; }
  &.cm-focused .cm-selectionBackground { background: $accent-mid !important; }
  .cm-activeLine { background: rgba(0,0,0,0.015); }
  .cm-gutters { display: none; }
}

.md-heading {
  font-family: $font-ui;
  font-weight: 700;
  color: $text;
  line-height: 1.25;
  letter-spacing: -0.02em;

  &--1 { font-size: 2.2em; margin: 0.6em 0 0.25em; }
  &--2 { font-size: 1.65em; margin: 0.5em 0 0.2em; }
  &--3 { font-size: 1.3em; margin: 0.4em 0 0.15em; }
  &--4 { font-size: 1.1em; margin: 0.3em 0 0.1em; }
  &--5 { font-size: 1em; }
  &--6 { font-size: 0.95em; color: $text-2; }
}

.md-heading-mark {
  color: $accent;
  opacity: 0.35;
  font-weight: 400;
  font-family: $font-mono;
  font-size: 0.65em;
  vertical-align: middle;
}

.md-bold { font-weight: 700; }
.md-italic { font-style: italic; }
.md-strikethrough { text-decoration: line-through; color: $text-3; }

.md-inline-code {
  font-family: $font-mono;
  font-size: 0.88em;
  background: $bg-code;
  padding: 1px 5px;
  border-radius: $r-sm;
  color: $text-code;
}

.md-syntax-hidden {
  font-size: 0;
  opacity: 0;
  width: 0;
  display: inline-block;
  overflow: hidden;
  transition: all $t-fast $ease;
}

.md-syntax-visible {
  font-size: inherit;
  opacity: 0.3;
  font-family: $font-mono;
  transition: all $t-fast $ease;
}

.md-code-block {
  font-family: $font-mono;
  font-size: $fs-sm;
  background: $bg-code-block;
  color: #d4d4d8;
  border-radius: $r-lg;
  padding: $sp-4;
  margin: $sp-2 0;
  .cm-line { padding: 0; }
}

.md-blockquote {
  border-left: 2px solid $accent;
  padding-left: $sp-4;
  color: $text-2;
  margin: $sp-2 0;
}

.md-blockquote-mark { color: $accent; opacity: 0.3; }

.md-link {
  color: $accent;
  text-decoration: none;
  border-bottom: 1px solid transparent;
  transition: border-color $t-fast $ease;
  cursor: pointer;
  &:hover { border-bottom-color: $accent; }
}

.md-image-widget {
  display: block;
  max-width: 100%;
  margin: $sp-4 0;
  border-radius: $r-md;
  transition: box-shadow $t-normal $ease;
  &:hover { box-shadow: $shadow-md; }
}

.md-image-placeholder {
  display: flex;
  align-items: center;
  justify-content: center;
  background: $bg-code;
  border: 1.5px dashed $border;
  border-radius: $r-md;
  padding: $sp-5;
  color: $text-3;
  font-size: $fs-sm;
  min-height: 100px;
  margin: $sp-2 0;
}

.md-hr {
  border: none;
  height: 1px;
  background: $border;
  margin: $sp-6 0;
}

.md-list-marker { color: $accent; font-weight: 600; }

.md-task-checkbox {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 16px;
  height: 16px;
  border: 1.5px solid $border;
  border-radius: 3px;
  margin-right: $sp-1;
  vertical-align: middle;
  transition: all $t-fast $ease;

  &--checked {
    background: $accent;
    border-color: $accent;
    color: #fff;
    &::after { content: '✓'; font-size: 10px; }
  }
}

.md-table-widget {
  width: 100%;
  border-collapse: collapse;
  margin: $sp-4 0;
  font-size: $fs-sm;

  th, td {
    border: 1px solid $border;
    padding: $sp-2 $sp-3;
    text-align: left;
  }
  th { background: $bg; font-weight: 600; }
}
```

##### `src/components/Toolbar.vue`

```vue
<template>
  <header class="toolbar">
    <div class="toolbar__left">
      <div class="toolbar__brand">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"/></svg>
        <span class="toolbar__name">Mira</span>
      </div>
      <span v-if="store.isDirty" class="toolbar__dot" />
    </div>

    <nav class="toolbar__actions">
      <div class="toolbar__group" v-for="(group, gi) in actionGroups" :key="gi">
        <button
          v-for="act in group"
          :key="act.id"
          class="toolbar__btn"
          :title="act.title"
          @click="emit('action', act.id)"
          v-html="act.icon"
        />
      </div>
    </nav>

    <div class="toolbar__right">
      <span class="toolbar__file">{{ store.fileName }}</span>
    </div>
  </header>
</template>

<script setup>
import { useEditorStore } from '@/stores/editor'

const store = useEditorStore()
const emit = defineEmits(['action'])

const I = (d, size = 16) =>
  `<svg width="${size}" height="${size}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">${d}</svg>`

const actionGroups = [
  [
    { id: 'bold', title: '粗体', icon: I('<path d="M6 4h8a4 4 0 0 1 4 4 4 4 0 0 1-4 4H6z"/><path d="M6 12h9a4 4 0 0 1 4 4 4 4 0 0 1-4 4H6z"/>') },
    { id: 'italic', title: '斜体', icon: I('<line x1="19" y1="4" x2="10" y2="4"/><line x1="14" y1="20" x2="5" y2="20"/><line x1="15" y1="4" x2="9" y2="20"/>') },
    { id: 'strikethrough', title: '删除线', icon: I('<path d="M16 4H9a3 3 0 0 0-2.83 4"/><path d="M14 12a4 4 0 0 1 0 8H6"/><line x1="4" y1="12" x2="20" y2="12"/>') },
  ],
  [
    { id: 'code', title: '行内代码', icon: I('<polyline points="16 18 22 12 16 6"/><polyline points="8 6 2 12 8 18"/>') },
    { id: 'link', title: '链接', icon: I('<path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"/><path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"/>') },
    { id: 'image', title: '图片', icon: I('<rect x="3" y="3" width="18" height="18" rx="2" ry="2"/><circle cx="8.5" cy="8.5" r="1.5"/><polyline points="21 15 16 10 5 21"/>') },
  ],
  [
    { id: 'blockquote', title: '引用', icon: I('<path d="M3 21c3 0 7-1 7-8V5c0-1.25-.756-2.017-2-2H4c-1.25 0-2 .75-2 1.972V11c0 1.25.75 2 2 2 1 0 1 0 1 1v1c0 1-1 2-2 2s-1 .008-1 1.031V21z"/><path d="M15 21c3 0 7-1 7-8V5c0-1.25-.757-2.017-2-2h-4c-1.25 0-2 .75-2 1.972V11c0 1.25.75 2 2 2h.75c0 2.25.25 4-2.75 4v3z"/>') },
    { id: 'bullet-list', title: '无序列表', icon: I('<line x1="8" y1="6" x2="21" y2="6"/><line x1="8" y1="12" x2="21" y2="12"/><line x1="8" y1="18" x2="21" y2="18"/><line x1="3" y1="6" x2="3.01" y2="6"/><line x1="3" y1="12" x2="3.01" y2="12"/><line x1="3" y1="18" x2="3.01" y2="18"/>') },
    { id: 'ordered-list', title: '有序列表', icon: I('<line x1="10" y1="6" x2="21" y2="6"/><line x1="10" y1="12" x2="21" y2="12"/><line x1="10" y1="18" x2="21" y2="18"/><path d="M4 6h1v4"/><path d="M4 10h2"/><path d="M6 18H4c0-1 2-2 2-3s-1-1.5-2-1"/>') },
    { id: 'hr', title: '分割线', icon: I('<line x1="2" y1="12" x2="22" y2="12"/>') },
  ],
]
</script>

<style lang="scss" scoped>
.toolbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 44px;
  padding: 0 $sp-4;
  background: $bg-elevated;
  border-bottom: 1px solid $border-light;
  user-select: none;
  flex-shrink: 0;
  z-index: $z-toolbar;

  &__left {
    display: flex;
    align-items: center;
    gap: $sp-2;
    min-width: 140px;
  }

  &__brand {
    display: flex;
    align-items: center;
    gap: 6px;
    color: $text;
  }

  &__name {
    font-size: $fs-sm;
    font-weight: 600;
    letter-spacing: -0.01em;
  }

  &__dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: $accent;
    flex-shrink: 0;
  }

  &__actions {
    display: flex;
    align-items: center;
    gap: $sp-1;
  }

  &__group {
    display: flex;
    align-items: center;
    gap: 1px;

    & + & {
      margin-left: $sp-2;
      padding-left: $sp-2;
      border-left: 1px solid $border-light;
    }
  }

  &__btn {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 30px;
    height: 30px;
    border: none;
    background: transparent;
    border-radius: $r-md;
    cursor: pointer;
    color: $text-2;
    transition: all $t-fast $ease;

    &:hover {
      background: $accent-soft;
      color: $accent;
    }
    &:active {
      transform: scale(0.93);
    }
  }

  &__right {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    min-width: 140px;
  }

  &__file {
    font-size: $fs-xs;
    color: $text-3;
    font-family: $font-mono;
  }
}
</style>
```

##### `src/components/EditorPane.vue`

```vue
<template>
  <div class="editor-pane" ref="editorContainer"></div>
</template>

<script setup>
import { ref, onMounted, onBeforeUnmount } from 'vue'
import { createEditor } from '@/editor'
import { useEditorStore } from '@/stores/editor'

const editorContainer = ref(null)
const store = useEditorStore()
let editorView = null
const emit = defineEmits(['ready'])

onMounted(() => {
  if (!editorContainer.value) return
  editorView = createEditor(editorContainer.value, {
    onUpdate(update) {
      if (update.docChanged) store.updateContent(update.state.doc.toString())
      if (update.selectionSet || update.docChanged) {
        const pos = update.state.selection.main.head
        const line = update.state.doc.lineAt(pos)
        store.updateCursor(line.number, pos - line.from + 1)
      }
    }
  })
  store.updateContent(editorView.state.doc.toString())
  emit('ready', editorView)
})

onBeforeUnmount(() => { editorView?.destroy(); editorView = null })

defineExpose({ getView: () => editorView })
</script>

<style lang="scss" scoped>
.editor-pane {
  flex: 1;
  overflow: hidden;
  background: $bg-editor;
}
</style>
```

##### `src/components/StatusBar.vue`

```vue
<template>
  <footer class="status">
    <span class="status__item">{{ store.statusText }}</span>
    <span class="status__sep">·</span>
    <span class="status__item">Markdown</span>
    <span class="status__sep">·</span>
    <span class="status__item">UTF-8</span>
  </footer>
</template>

<script setup>
import { useEditorStore } from '@/stores/editor'
const store = useEditorStore()
</script>

<style lang="scss" scoped>
.status {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 24px;
  gap: $sp-2;
  background: $bg-elevated;
  border-top: 1px solid $border-light;
  flex-shrink: 0;

  &__item {
    font-size: $fs-xs;
    color: $text-3;
    font-family: $font-mono;
  }

  &__sep {
    font-size: $fs-xs;
    color: $border;
  }
}
</style>
```

##### `src/App.vue`

```vue
<template>
  <div class="app">
    <Toolbar @action="handleToolbarAction" />
    <EditorPane ref="editorPane" @ready="onEditorReady" />
    <StatusBar />
    <Transition name="toast">
      <div v-if="toast.visible" :class="['toast', `toast--${toast.type}`]">
        {{ toast.message }}
      </div>
    </Transition>
  </div>
</template>

<script setup>
import { ref, reactive } from 'vue'
import Toolbar from '@/components/Toolbar.vue'
import EditorPane from '@/components/EditorPane.vue'
import StatusBar from '@/components/StatusBar.vue'

const editorPane = ref(null)
let editorView = null

const toast = reactive({ visible: false, message: '', type: 'info' })
let toastTimer = null

function showToast(msg, type = 'info') {
  toast.message = msg; toast.type = type; toast.visible = true
  if (toastTimer) clearTimeout(toastTimer)
  toastTimer = setTimeout(() => { toast.visible = false }, 2000)
}

function onEditorReady(view) { editorView = view }

function insertText(before, after = '') {
  if (!editorView) return
  const { from, to } = editorView.state.selection.main
  const sel = editorView.state.sliceDoc(from, to)
  const text = `${before}${sel || 'text'}${after}`
  editorView.dispatch({
    changes: { from, to, insert: text },
    selection: { anchor: from + before.length, head: from + before.length + (sel || 'text').length }
  })
  editorView.focus()
}

function insertLine(prefix) {
  if (!editorView) return
  const line = editorView.state.doc.lineAt(editorView.state.selection.main.head)
  editorView.dispatch({ changes: { from: line.from, to: line.from, insert: prefix } })
  editorView.focus()
}

function handleToolbarAction(action) {
  const map = {
    bold: () => insertText('**', '**'),
    italic: () => insertText('*', '*'),
    strikethrough: () => insertText('~~', '~~'),
    code: () => insertText('`', '`'),
    link: () => insertText('[', '](url)'),
    image: () => insertText('![alt](', ')'),
    blockquote: () => insertLine('> '),
    'bullet-list': () => insertLine('- '),
    'ordered-list': () => insertLine('1. '),
    hr: () => {
      const pos = editorView.state.selection.main.head
      const line = editorView.state.doc.lineAt(pos)
      editorView.dispatch({ changes: { from: line.to, to: line.to, insert: '\n\n---\n\n' } })
      editorView.focus()
    },
  }
  const fn = map[action]
  fn ? fn() : showToast(`未知操作: ${action}`, 'warning')
}
</script>

<style lang="scss" scoped>
.app {
  display: flex;
  flex-direction: column;
  height: 100vh;
  background: $bg;
}

.toast {
  position: fixed;
  bottom: 40px;
  left: 50%;
  transform: translateX(-50%);
  padding: $sp-2 $sp-5;
  border-radius: $r-full;
  font-size: $fs-sm;
  color: #fff;
  z-index: $z-toast;
  box-shadow: $shadow-lg;
  pointer-events: none;
  font-family: $font-ui;

  &--info { background: $accent; }
  &--success { background: $success; }
  &--warning { background: $warning; }
  &--error { background: $error; }
}

.toast-enter-active,
.toast-leave-active {
  transition: all $t-slow $ease;
}
.toast-enter-from,
.toast-leave-to {
  opacity: 0;
  transform: translateX(-50%) translateY(8px);
}
</style>
```

##### `src/editor/theme.js`

```javascript
import { EditorView } from '@codemirror/view'

export const editorBaseTheme = EditorView.baseTheme({
  '&': { height: '100%', backgroundColor: '#ffffff' },
  '&.cm-focused': { outline: 'none' },
  '.cm-scroller': { fontFamily: 'inherit' },
  '.cm-content': { caretColor: '#2563eb' },
  '.cm-cursor, .cm-dropCursor': { borderLeftColor: '#2563eb', borderLeftWidth: '1.8px' }
})
```

##### `index.html`

```html
<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Mira — Markdown Editor</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.js"></script>
  </body>
</html>
```

#### 验证结果

- `npm run build` ✅ 构建成功


---

## 对话 3：图片路径优化

### 用户

用中文进行沟通，将我们的所有对话（包括之后的每一次对话）追加到label-02141.md中（包括生成的代码）就行以下优化：插入图片写入本地路径图片显示错误示范(输入内容为：![C:\Users\benzhi\Desktop\BenZhiTec](错误示范))，请分析，是用户输入有问题还是代码还有优化的空间，可以优化的话进行优化，不可以实现的话请告诉我原因，并且在 README.md 文件中对于该输入的异常情况和原因进行说明

### AI 回复

#### 问题分析

用户输入：`![C:\Users\benzhi\Desktop\BenZhiTec](错误示范)`

这个输入存在两个问题：

1. **Markdown 语法错误**
   - 正确语法：`![替代文本](图片地址)`
   - 用户输入：`![图片地址](替代文本)` ← 位置颠倒了
   - 方括号 `[]` 应该放替代文本（alt text）
   - 圆括号 `()` 应该放图片地址（URL）

2. **本地路径限制**
   - 即使语法正确，`C:\Users\benzhi\Desktop\BenZhiTec` 这样的本地文件系统路径在浏览器中也无法访问
   - 原因：浏览器安全策略禁止网页直接访问用户本地文件系统
   - 这是所有现代浏览器的安全限制，无法通过前端代码绕过

#### 优化方案

虽然无法让浏览器直接访问本地路径，但可以优化用户体验：

1. **增强路径识别**：识别 Windows/Mac/Linux 各种本地路径格式
2. **友好错误提示**：用图标和文字清晰说明为什么无法显示
3. **提供解决方案**：在提示中告知用户正确的使用方式
4. **完善文档**：在 README 中详细说明图片路径的限制和解决方案

#### 实现的优化

##### 1. 增强的 ImageWidget（`frontend-editor/src/editor/decoration-plugin.js`）

```javascript
class ImageWidget extends WidgetType {
  constructor(alt, url) {
    super()
    this.alt = alt
    this.url = url
  }

  /**
   * 检测是否为本地文件系统路径
   */
  isLocalPath(path) {
    if (!path) return false
    // Windows: C:\, D:\, \\network\path
    if (/^[a-zA-Z]:\\/.test(path) || path.startsWith('\\\\')) return true
    // Unix/Mac: /absolute/path or ~/home/path or file://
    if (path.startsWith('file://') || (path.startsWith('/') && !path.startsWith('//'))) return true
    // Relative paths: ./path or ../path
    if (path.startsWith('./') || path.startsWith('../')) return true
    return false
  }

  /**
   * 检测是否为网络路径
   */
  isNetworkPath(path) {
    if (!path) return false
    return /^https?:\/\//i.test(path) || path.startsWith('//')
  }

  toDOM() {
    const wrapper = document.createElement('span')
    wrapper.className = 'md-image-placeholder'

    // 检查是否为网络路径
    if (this.isNetworkPath(this.url)) {
      const img = document.createElement('img')
      img.src = this.url
      img.alt = this.alt || ''
      img.className = 'md-image-widget'
      img.style.maxWidth = '100%'
      img.onerror = () => {
        wrapper.innerHTML = `
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <rect x="3" y="3" width="18" height="18" rx="2" ry="2"/>
            <circle cx="8.5" cy="8.5" r="1.5"/>
            <polyline points="21 15 16 10 5 21"/>
          </svg>
          <span style="margin-left: 8px;">图片加载失败: ${this.alt || this.url}</span>
        `
        wrapper.className = 'md-image-placeholder'
      }
      wrapper.textContent = ''
      wrapper.className = ''
      wrapper.appendChild(img)
    } 
    // 检查是否为本地文件系统路径
    else if (this.isLocalPath(this.url)) {
      wrapper.innerHTML = `
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/>
          <line x1="12" y1="9" x2="12" y2="13"/>
          <line x1="12" y1="17" x2="12.01" y2="17"/>
        </svg>
        <span style="margin-left: 8px;">浏览器无法访问本地路径: ${this.url}</span>
        <br/>
        <span style="font-size: 11px; opacity: 0.7;">提示：请使用 http:// 或 https:// 开头的网络图片地址</span>
      `
    } 
    // 其他情况（可能是相对路径或无效路径）
    else {
      wrapper.innerHTML = `
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <rect x="3" y="3" width="18" height="18" rx="2" ry="2"/>
          <circle cx="8.5" cy="8.5" r="1.5"/>
          <polyline points="21 15 16 10 5 21"/>
        </svg>
        <span style="margin-left: 8px;">${this.alt || '图片'}: ${this.url || '未指定路径'}</span>
      `
    }
    return wrapper
  }
  ignoreEvent() { return false }
  eq(other) { return other.url === this.url && other.alt === this.alt }
}
```

##### 2. 优化的占位符样式（`frontend-editor/src/styles/editor-theme.scss`）

```scss
.md-image-placeholder {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  background: $bg-code;
  border: 1.5px dashed $border;
  border-radius: $r-md;
  padding: $sp-5;
  color: $text-3;
  font-size: $fs-sm;
  min-height: 100px;
  margin: $sp-2 0;
  text-align: center;
  line-height: 1.6;

  svg {
    flex-shrink: 0;
  }

  span {
    word-break: break-all;
  }
}
```

##### 3. README 文档更新

在 README.md 中新增了完整的"图片插入说明"章节，包括：

- 支持的图片格式
- 图片路径类型（网络图片 vs 本地路径）
- 为什么不支持本地路径的技术原因
- 解决方案（图床、本地服务器、Base64）
- 常见错误示例和正确用法
- 错误提示说明

#### 优化效果

1. **用户输入 `![C:\Users\benzhi\Desktop\BenZhiTec](错误示范)`**
   - 编辑器会识别出这是 Windows 本地路径
   - 显示警告图标 ⚠️ 和友好提示："浏览器无法访问本地路径"
   - 提供解决建议："请使用 http:// 或 https:// 开头的网络图片地址"

2. **网络图片加载失败**
   - 显示图片图标 🖼️ 和错误信息
   - 保留 alt 文本供用户参考

3. **无效或空路径**
   - 显示占位符和路径信息
   - 帮助用户快速定位问题

#### 总结

这个问题既有用户输入的问题（语法错误），也有技术限制（浏览器安全策略）。通过代码优化，我们提供了更好的错误提示和用户引导，虽然无法突破浏览器的安全限制，但能让用户清楚地知道问题所在和如何解决。


---

## 对话 4：优化图片加载失败样式

### 用户

优化图片加载失败的样式，将图片的 icon 和图片加载失败要在一个水平线显示

### AI 回复

已优化图片加载失败时的显示样式，现在图标和文字在同一水平线上显示。

#### 优化内容

修改了 `frontend-editor/src/editor/decoration-plugin.js` 中的 `ImageWidget` 类，使用 flexbox 布局让图标和文字水平对齐：

**1. 图片加载失败（网络图片）**
```javascript
img.onerror = () => {
  wrapper.innerHTML = `
    <div style="display: flex; align-items: center; gap: 8px;">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <rect x="3" y="3" width="18" height="18" rx="2" ry="2"/>
        <circle cx="8.5" cy="8.5" r="1.5"/>
        <polyline points="21 15 16 10 5 21"/>
      </svg>
      <span>图片加载失败: ${this.alt || this.url}</span>
    </div>
  `
  wrapper.className = 'md-image-placeholder'
}
```

**2. 本地路径警告**
```javascript
wrapper.innerHTML = `
  <div style="display: flex; flex-direction: column; gap: 8px; align-items: center;">
    <div style="display: flex; align-items: center; gap: 8px;">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/>
        <line x1="12" y1="9" x2="12" y2="13"/>
        <line x1="12" y1="17" x2="12.01" y2="17"/>
      </svg>
      <span>浏览器无法访问本地路径: ${this.url}</span>
    </div>
    <span style="font-size: 11px; opacity: 0.7;">提示：请使用 http:// 或 https:// 开头的网络图片地址</span>
  </div>
`
```

**3. 无效路径**
```javascript
wrapper.innerHTML = `
  <div style="display: flex; align-items: center; gap: 8px;">
    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <rect x="3" y="3" width="18" height="18" rx="2" ry="2"/>
      <circle cx="8.5" cy="8.5" r="1.5"/>
      <polyline points="21 15 16 10 5 21"/>
    </svg>
    <span>${this.alt || '图片'}: ${this.url || '未指定路径'}</span>
  </div>
`
```

#### 优化效果

- 使用 `display: flex` 和 `align-items: center` 确保图标和文字垂直居中对齐
- 使用 `gap: 8px` 设置图标和文字之间的间距
- 本地路径警告使用嵌套 flex 布局，主提示水平显示，辅助提示在下方居中显示
- 所有错误提示的图标和主文字都在同一水平线上，视觉效果更加统一和专业

---

## 对话 5：补强格式化命令

### 用户

补强格式化命令：对有选区、无选区、跨行选区分别给出确定结果，连续执行同类格式时应切换而不是无限叠加，并让按钮状态反映当前光标位置。遇到未知格式或未知命令时必须保持原内容，撤销、快捷键与按钮混用及切换文稿后不能污染其它区域；新命令还要与字数统计、保存提示和实时渲染同步。

### AI 回复

#### 问题分析与设计约定

旧实现（App.vue 中的 `insertText` / `insertLine`）存在以下缺陷：

1. 无论光标态还是选区，只是简单地在两端插入标记，连续点两次粗体会得到 `****text****`，无限叠加；
2. 没有选区时插入硬编码的 `text` 字样，且不选中文本，用户必须手动删除；
3. 跨行选区的行为未定义（只支持单行）；
4. 未知操作仅弹 toast，但没有"不改内容"的契约保障；
5. 工具栏按钮没有激活态，无法反映光标当前所在的语法区域；
6. 按钮逻辑写在 Vue 组件里，无法被键盘快捷键复用，两者行为可能漂移；
7. 没有文稿切换的隔离机制。

因此新增一个与 UI 解耦的纯函数命令引擎 `src/editor/commands.js`，所有格式化（按钮、快捷键）都走同一个入口；命令返回的是可 dispatch 的事务规格，未知命令返回 `null`，调用方一律不派发事务。

**确定性行为约定：**

| 场景 | 行内格式（粗体/斜体/删除线/代码） |
|------|----------------------------------|
| 无选区 | 光标已在该格式区域内 → 移除标记（切换）；否则插入 `标记+占位符+标记` 并选中占位符 |
| 有选区（单行/跨行同一规则） | 选区恰好被标记包裹 → 解包；否则包裹，选中包裹后的内容 |
| 连续执行 | 始终在"包裹 ↔ 解包"间交替，绝不叠加 `****` |
| 斜体 `*` | 用同字符边界 lookaround 保证不误吃粗体的 `**` |

| 场景 | 行级格式（引用/无序/有序列表） |
|------|-------------------------------|
| 多行选区 | 对选区触及的每个非空行生效，空行跳过 |
| 全部已有前缀 | 移除（切换） |
| 部分有前缀 | 补齐；有序列表对所有触及行按序重编号，无序/有序互转时标记规范化 |

**隔离与同步保证：**

- 每个命令只派发**一个事务** → CodeMirror history 中一次 Ctrl/Cmd+Z 即可完整回退；
- 快捷键（Mod-B/I/E/K、Mod-Shift-X）与按钮调用同一个 `runFormatCommand`，行为不可能漂移；
- 未知命令返回 `false`，不 dispatch，文档逐字节不变；
- 切换/新建文稿用 `view.setState(createEditorState(...))` 整体替换，撤销栈、装饰插件状态、选区全部归零，并通过 store 的 `loadDocument()` 重置统计与激活格式；
- 所有变更都经由 view dispatch 触发 `updateListener` → 字数统计、保存提示（脏点）、按钮激活态、实时渲染装饰在同一次更新中同步刷新。

#### 生成的代码

##### `frontend-editor/src/editor/commands.js`（新增：纯函数命令引擎）

```javascript
/**
 * 格式化命令引擎（纯函数）
 *
 * 设计约定（确定性行为）：
 * - 无选区（光标态）：光标已处于该格式区域内 -> 移除标记（切换）；
 *                    否则在光标处插入 `标记+占位符+标记` 并选中占位符。
 * - 有选区（单行或跨行，同一套规则）：选区恰好被该标记包裹 -> 移除标记；
 *                    否则在选区两端插入标记，选中包裹后的内容。
 * - 行级格式（引用/列表）：作用于选区触及的每一个非空行；
 *                    全部已有该前缀 -> 移除，否则补齐（有序列表按序重编号）。
 * - 未知格式 / 未知命令：返回 null，调用方不得派发任何事务，原内容保持不变。
 * - 每个命令只派发一个事务，因此一次撤销即可完整回退。
 */

export const INLINE_FORMATS = {
  bold: { mark: '**', placeholder: '粗体文本' },
  italic: { mark: '*', placeholder: '斜体文本' },
  strikethrough: { mark: '~~', placeholder: '删除线文本' },
  code: { mark: '`', placeholder: '代码' }
}

const LINK_TEXT_PLACEHOLDER = '链接文字'
const LINK_URL_PLACEHOLDER = 'https://example.com'
const IMG_ALT_PLACEHOLDER = '图片描述'
const IMG_URL_PLACEHOLDER = 'https://example.com/image.png'

export const LINE_FORMAT_IDS = ['blockquote', 'bullet-list', 'ordered-list']
export const INSERT_ONLY_IDS = ['image', 'hr', 'new-document']

function escapeRe(s) {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')
}

// “恰好由 mark 包裹”的正则：标记两侧不允许紧接同字符，
// 例如斜体 `*` 不会误匹配粗体的 `**`。
function wrappedPattern(mark) {
  const e = escapeRe(mark)
  const ch = escapeRe(mark[0])
  return new RegExp(
    `(?<!${ch})${e}(?!${ch})(.+?)(?<!${ch})${e}(?!${ch})`,
    'g'
  )
}

// 选区外侧是否紧贴一对完整标记（标记的另一侧不能是同字符，避免 * 误吃 **）
function hasOuterMarks(state, from, to, mark) {
  const ch = mark[0]
  const before = state.sliceDoc(from - mark.length, from)
  const after = state.sliceDoc(to, to + mark.length)
  if (before !== mark || after !== mark) return false
  const before2 = state.sliceDoc(from - mark.length - 1, from - mark.length)
  const after2 = state.sliceDoc(to + mark.length, to + mark.length + 1)
  return before2 !== ch && after2 !== ch
}

// 选区文本自身是否就是 “标记+内容+标记”
function hasInnerMarks(text, mark) {
  const ch = mark[0]
  return (
    text.length >= mark.length * 2 &&
    text.startsWith(mark) &&
    text.endsWith(mark) &&
    text[mark.length] !== ch &&
    text[text.length - mark.length - 1] !== ch
  )
}

// 在光标所在行查找包含 pos 的行内格式区域
function findEnclosingInline(state, pos, mark) {
  const line = state.doc.lineAt(pos)
  const re = wrappedPattern(mark)
  let m
  while ((m = re.exec(line.text)) !== null) {
    const from = line.from + m.index
    const to = from + m[0].length
    if (pos >= from && pos <= to) {
      return { from, to, contentFrom: from + mark.length, contentTo: to - mark.length }
    }
  }
  return null
}

function findEnclosingLink(state, pos) {
  const line = state.doc.lineAt(pos)
  const re = /(?<!!)\[([^\]]*)\]\(([^)]*)\)/g
  let m
  while ((m = re.exec(line.text)) !== null) {
    const from = line.from + m.index
    const to = from + m[0].length
    if (pos >= from && pos <= to) return { from, to }
  }
  return null
}

export function toggleInlineFormat(state, formatId) {
  const fmt = INLINE_FORMATS[formatId]
  if (!fmt) return null
  const { mark, placeholder } = fmt
  const range = state.selection.main

  // —— 无选区：光标态 ——
  if (range.empty) {
    const region = findEnclosingInline(state, range.head, mark)
    if (region) {
      const pos = range.head
      const openBefore = pos > region.from ? Math.min(mark.length, pos - region.from) : 0
      const closeBefore = pos > region.to - mark.length ? pos - (region.to - mark.length) : 0
      const newPos = pos - openBefore - closeBefore
      return {
        changes: [
          { from: region.from, to: region.contentFrom },
          { from: region.contentTo, to: region.to }
        ],
        selection: { anchor: newPos }
      }
    }
    const insert = `${mark}${placeholder}${mark}`
    return {
      changes: { from: range.head, insert },
      selection: {
        anchor: range.head + mark.length,
        head: range.head + mark.length + placeholder.length
      }
    }
  }

  // —— 有选区（单行/跨行规则一致）——
  const { from, to } = range
  const selText = state.sliceDoc(from, to)

  if (hasOuterMarks(state, from, to, mark)) {
    return {
      changes: [
        { from: from - mark.length, to: from },
        { from: to, to: to + mark.length }
      ],
      selection: { anchor: from - mark.length, head: to - mark.length }
    }
  }

  if (hasInnerMarks(selText, mark)) {
    return {
      changes: [
        { from: from, to: from + mark.length },
        { from: to - mark.length, to }
      ],
      selection: { anchor: from, head: to - mark.length * 2 }
    }
  }

  return {
    changes: [
      { from, insert: mark },
      { from: to, insert: mark }
    ],
    selection: { anchor: from + mark.length, head: to + mark.length }
  }
}

// 链接切换：光标处于链接内 / 选中整条链接 -> 还原纯文本；否则插入/包裹链接骨架
export function toggleLink(state) {
  const range = state.selection.main
  if (range.empty) {
    const region = findEnclosingLink(state, range.head)
    if (region) {
      const m = state.sliceDoc(region.from, region.to)
        .match(/^\[([^\]]*)\]\(([^)]*)\)$/)
      if (m) {
        return {
          changes: { from: region.from, to: region.to, insert: m[1] },
          selection: { anchor: region.from + m[1].length }
        }
      }
    }
  }
  return buildLinkSpec(state, range)
}

function buildLinkSpec(state, range) {
  const { from, to } = range
  if (range.empty) {
    const insert = `[${LINK_TEXT_PLACEHOLDER}](${LINK_URL_PLACEHOLDER})`
    return {
      changes: { from, insert },
      selection: { anchor: from + 1, head: from + 1 + LINK_TEXT_PLACEHOLDER.length }
    }
  }
  const selText = state.sliceDoc(from, to)
  const m = selText.match(/^\[([^\]]*)\]\(([^)]*)\)$/)
  if (m) {
    return {
      changes: { from, to, insert: m[1] },
      selection: { anchor: from, head: from + m[1].length }
    }
  }
  const insert = `[${selText}](${LINK_URL_PLACEHOLDER})`
  return {
    changes: { from, to, insert },
    selection: { anchor: to + 3, head: to + 3 + LINK_URL_PLACEHOLDER.length }
  }
}

// 图片插入（仅插入，不切换）：选区作为 alt，否则插入占位骨架
export function insertImage(state) { /* 见仓库源码 src/editor/commands.js */ }

// 取选区覆盖的行；若选区端点正好落在下一行行首（from < to），不包含该空行
function linesTouchedBySelection(state) {
  const { from, to } = state.selection.main
  const first = state.doc.lineAt(from)
  const last = state.doc.lineAt(to)
  let lastLineNo = last.number
  if (to > from && to === last.from && last.number > first.number) {
    lastLineNo = last.number - 1
  }
  const lines = []
  for (let n = first.number; n <= lastLineNo; n++) lines.push(state.doc.line(n))
  return lines
}

const BLOCKQUOTE_RE = /^>\s?/
const BULLET_RE = /^(\s*)[-*+]\s/
const ORDERED_RE = /^(\s*)\d+\.\s/
const INDENT_RE = /^\s*/
const isNonEmpty = line => line.text.trim().length > 0

function buildLineChanges(state, id) {
  const lines = linesTouchedBySelection(state).filter(isNonEmpty)
  if (lines.length === 0) return null
  const changes = []

  if (id === 'blockquote') {
    const allHave = lines.every(l => BLOCKQUOTE_RE.test(l.text))
    for (const line of lines) {
      const m = line.text.match(BLOCKQUOTE_RE)
      if (allHave && m) changes.push({ from: line.from, to: line.from + m[0].length })
      else if (!m) changes.push({ from: line.from, insert: '> ' })
    }
    return { changes }
  }

  if (id === 'bullet-list') {
    const allBullet = lines.every(l => BULLET_RE.test(l.text))
    for (const line of lines) {
      if (allBullet) {
        const bm = line.text.match(BULLET_RE)
        if (bm) changes.push({ from: line.from, to: line.from + bm[0].length, insert: bm[1] })
        continue
      }
      const marker = line.text.match(BULLET_RE) || line.text.match(ORDERED_RE)
      if (marker) {
        changes.push({ from: line.from, to: line.from + marker[0].length, insert: `${marker[1]}- ` })
      } else {
        changes.push({ from: line.from + line.text.match(INDENT_RE)[0].length, insert: '- ' })
      }
    }
    return { changes }
  }

  if (id === 'ordered-list') {
    const allOrdered = lines.every(l => ORDERED_RE.test(l.text))
    let num = 1
    for (const line of lines) {
      if (allOrdered) {
        const om = line.text.match(ORDERED_RE)
        if (om) changes.push({ from: line.from, to: line.from + om[0].length, insert: om[1] })
        continue
      }
      const marker = line.text.match(ORDERED_RE) || line.text.match(BULLET_RE)
      if (marker) {
        changes.push({ from: line.from, to: line.from + marker[0].length, insert: `${marker[1]}${num}. ` })
      } else {
        const indent = line.text.match(INDENT_RE)[0]
        changes.push({ from: line.from + indent.length, insert: `${num}. ` })
      }
      num++
    }
    return { changes }
  }

  return null
}

export function toggleLineFormat(state, id) {
  if (!LINE_FORMAT_IDS.includes(id)) return null
  return buildLineChanges(state, id)
}

// 水平分割线：插入到当前行之后，光标落到分割线后的空行
export function insertHorizontalRule(state) {
  const line = state.doc.lineAt(state.selection.main.head)
  const insert = '\n\n---\n\n'
  return {
    changes: { from: line.to, insert },
    selection: { anchor: line.to + insert.length }
  }
}

// 命令总入口：未知命令返回 null（保持原内容）
export function formatSpec(state, commandId) {
  if (INLINE_FORMATS[commandId]) return toggleInlineFormat(state, commandId)
  if (commandId === 'link') return toggleLink(state)
  if (commandId === 'image') return insertImage(state)
  if (LINE_FORMAT_IDS.includes(commandId)) return buildLineChanges(state, commandId)
  if (commandId === 'hr') return insertHorizontalRule(state)
  return null
}

// 派发格式化命令。false 表示未知命令，不派发任何事务，文档保持原样。
export function runFormatCommand(view, commandId) {
  const spec = formatSpec(view.state, commandId)
  if (!spec) return false
  view.dispatch(spec)
  view.focus()
  return true
}

// 计算当前选区/光标下激活的格式 id，用于工具栏按钮高亮
export function getActiveFormats(state) {
  const active = []
  const range = state.selection.main
  const { from, to } = range

  if (range.empty) {
    for (const [id, fmt] of Object.entries(INLINE_FORMATS)) {
      if (findEnclosingInline(state, to, fmt.mark)) active.push(id)
    }
    if (findEnclosingLink(state, to)) active.push('link')
  } else {
    const selText = state.sliceDoc(from, to)
    for (const [id, fmt] of Object.entries(INLINE_FORMATS)) {
      if (hasOuterMarks(state, from, to, fmt.mark) || hasInnerMarks(selText, fmt.mark)) {
        active.push(id)
      }
    }
    if (/^\[([^\]]*)\]\(([^)]*)\)$/.test(selText)) active.push('link')
  }

  const touched = linesTouchedBySelection(state).filter(isNonEmpty)
  if (touched.length > 0) {
    if (touched.every(l => BLOCKQUOTE_RE.test(l.text))) active.push('blockquote')
    if (touched.every(l => BULLET_RE.test(l.text))) active.push('bullet-list')
    if (touched.every(l => ORDERED_RE.test(l.text))) active.push('ordered-list')
  }

  return active
}
```

> 完整源码（含 `insertImage`）以仓库 `frontend-editor/src/editor/commands.js` 为准。

##### `frontend-editor/src/editor/setup.js`（改造要点）

- 抽出 `createEditorState(doc, onUpdate)`，供切换文稿时整体替换（全新 history、装饰插件、选区）；
- 新增快捷键，与按钮共用 `runFormatCommand`：

```javascript
keymap.of([
  { key: 'Mod-b', run: view => runFormatCommand(view, 'bold') },
  { key: 'Mod-i', run: view => runFormatCommand(view, 'italic') },
  { key: 'Mod-e', run: view => runFormatCommand(view, 'code') },
  { key: 'Mod-k', run: view => runFormatCommand(view, 'link') },
  { key: 'Mod-Shift-x', run: view => runFormatCommand(view, 'strikethrough') },
  ...defaultKeymap,
  ...historyKeymap,
  indentWithTab
]),

export function createEditorState(doc, onUpdate) {
  // extensions 组装（同上，含 history 与 markdownDecorationPlugin）
  return EditorState.create({ doc: doc === undefined ? defaultContent : doc, extensions })
}

export function createEditor(parent, options = {}) {
  return new EditorView({ state: createEditorState(options.doc, options.onUpdate), parent })
}
```

##### `frontend-editor/src/components/EditorPane.vue`（改造要点）

```javascript
import { createEditor, createEditorState } from '@/editor'
import { getActiveFormats } from '@/editor/commands'

// 文档/选区变更统一走这里：字数、脏标记、光标位置、按钮激活态同源同步
function handleUpdate(update) {
  if (update.docChanged) store.updateContent(update.state.doc.toString())
  if (update.selectionSet || update.docChanged) {
    const pos = update.state.selection.main.head
    const line = update.state.doc.lineAt(pos)
    store.updateCursor(line.number, pos - line.from + 1)
    store.setActiveFormats(getActiveFormats(update.state))
  }
}

// 整体替换文稿：全新 EditorState（含全新 history/装饰状态），
// 上一份文稿的撤销栈、选区、激活格式不会污染新文稿
function loadDocument(doc = '', name = 'untitled.md') {
  if (!editorView) return
  editorView.setState(createEditorState(doc, handleUpdate))
  store.loadDocument(editorView.state.doc.toString(), name)
  editorView.focus()
}

defineExpose({ getView: () => editorView, loadDocument })
```

##### `frontend-editor/src/stores/editor.js`（新增部分）

```javascript
const activeFormats = ref([])

// 仅在激活格式变化时更新，避免每次选区更新都触发工具栏重渲染
function setActiveFormats(list) {
  if (list.length === activeFormats.value.length &&
      list.every((id, i) => id === activeFormats.value[i])) return
  activeFormats.value = list
}

// 切换/新建文稿统一重置入口，不残留上一份文稿的状态
function loadDocument(newContent, name = 'untitled.md') {
  content.value = newContent
  fileName.value = name
  isDirty.value = false
  charCount.value = newContent.length
  lineCount.value = newContent.split('\n').length
  wordCount.value = newContent.trim() ? newContent.trim().split(/\s+/).length : 0
  cursorLine.value = 1
  cursorCol.value = 1
  activeFormats.value = []
}
```

##### `frontend-editor/src/App.vue`（handleToolbarAction 改为统一入口）

```javascript
import { runFormatCommand } from '@/editor/commands'

function handleToolbarAction(action) {
  if (action === 'new-document') {
    editorPane.value?.loadDocument('', 'untitled.md')
    showToast('已新建文稿', 'success')
    return
  }
  if (!editorView) return
  // 返回 false 表示未知命令：不派发任何事务，原内容保持不变
  const handled = runFormatCommand(editorView, action)
  if (!handled) showToast(`未知操作: ${action}`, 'warning')
}
```

##### `frontend-editor/src/components/Toolbar.vue`（改造要点）

- 按钮按 `store.activeFormats.includes(act.id)` 增加 `toolbar__btn--active` 激活态（蓝底白字）；
- 标题附带快捷键说明（粗体 Ctrl/Cmd+B 等）；
- 右侧新增"新建文稿"按钮（file-plus 线性图标），派发 `new-document`。

```scss
&__btn--active,
&__btn--active:hover {
  background: $accent;
  color: #fff;
}
```

##### `frontend-editor/tests/commands.test.mjs`（新增：32 项确定性测试）

纯 Node 可运行（`npm test`），基于真实的 `@codemirror/state` EditorState，覆盖：

- 粗体：无选区插入占位并选中、光标在格式内解包、连按两次/三次在包裹↔解包交替（绝不出现 `****`）；
- 有选区：标记在选区外解包、选区含标记剥离、包裹后选中内容；
- 跨行选区：整体包裹与解包结果确定；
- 斜体不误吃粗体 `**`；删除线/代码切换；
- 未知格式 id → `formatSpec` 返回 `null`；未知命令 → 状态引用不变、内容逐字节不变；
- 引用多行添加/切换移除/混合补齐/跳过空行；
- 无序、有序列表添加与移除；有序→无序标记规范化、无序→有序统一重编号；
- 链接：选区包裹并选中 URL、光标在链接内还原、整条链接还原、无选区占位；
- 图片插入、水平分割线插入位置与光标落位；
- `getActiveFormats`：光标/选区/多行场景的按钮激活态。

#### 验证结果

- `npm test`（`node tests/commands.test.mjs`）✅ 全部 32 项测试通过
- `npm run build` ✅ 构建成功

#### 同步性说明

所有格式化命令都通过 `view.dispatch()` 生效，因此与既有机制天然同步：

- **字数统计**：`update.docChanged` 触发 `store.updateContent()`，words/chars/lines 同步；
- **保存提示**：脏点随 `isDirty` 出现；新建/切换文稿经 `loadDocument()` 归位为非脏状态；
- **实时渲染**：CodeMirror 的 `markdownDecorationPlugin` 在每次文档更新后重新计算装饰，包裹/解包标记立即在"语法标记 ↔ 渲染效果"间切换；
- **撤销**：一个命令一个事务，Ctrl/Cmd+Z 一次回退整条命令；按钮与快捷键走同一入口，混用不产生差异；
- **文稿隔离**：`view.setState()` 整体替换，旧文稿的撤销栈、选区、装饰、激活格式无法污染新文稿。
